# GSD Website (Eleventy)

This folder contains an Eleventy (11ty) site used to promote and document **GET SHIT DONE (GSD)**.

## Local development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The output is written to `dist/`.
